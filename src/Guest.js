import React, { Component } from "react";

export default class Guest extends Component {
  render() {
    return (
      <div>
        <h1>Welcome to ,This is Guest Protal </h1>
        <button type="Button"> Login </button>
        <button type="Button"> Logout </button>
      </div>
    );
  }
}
